model:
  embedding_dim: 128
  lstm_units: 128
  dropout: 0.3
  num_classes: 5

training:
  batch_size: 32
  epochs: 10
  learning_rate: 0.001

data:
  max_length: 50
  vocab_size: 10000
  test_split: 0.2